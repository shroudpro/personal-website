# 静态个人博客与作品集产品开发文档

更新时间：2026-05-04

## 1. 产品定位

本项目定位为一个基于 Vue3 + Vite + TypeScript 的静态个人博客与作品集网站，用于对外展示 Shroud 的个人介绍、项目作品、成长经历、成就记录和阶段性思考，也用于本地维护个人成长记录。

网站的核心使用方式是：

```txt
本地编辑内容
  ↓
运行测试与构建
  ↓
推送到 GitHub
  ↓
Netlify 自动部署
  ↓
对外展示最新版本
```

当前版本不开发线上后端、不接线上数据库、不做登录、不做可视化后台、不做访客交互。内容维护以本地文件为主，改完后重新部署到 Netlify。

## 2. 当前选择结论

1. 项目最终定位为“静态个人博客 + 作品集”。
   - 对外展示个人能力、项目、经历和成长记录。
   - 对内作为本地维护的个人成长记录系统。

2. 后端和数据库移入“未来扩展方向”。
   - 当前版本不部署 FastAPI 后端。
   - 当前版本不使用 Neon、Supabase 或其他线上数据库。
   - 现有后端代码如果保留，只作为未来扩展或本地实验，不作为当前主线目标。

3. Experience 使用 Markdown 文件维护。
   - 每条成长经历或学习记录可以使用一个 Markdown 文件。
   - 页面通过构建期或本地数据映射方式展示。

4. Project 使用 Markdown 文件维护。
   - 每个项目一个 Markdown 文件。
   - 项目详情页优先从项目 Markdown 内容渲染。
   - 项目列表可由项目元信息生成。

5. Achievement 继续使用 TypeScript 数据文件维护。
   - 保持 `src/data/achievements.ts`。
   - 补充字段说明和维护规范。

6. 全局中英文切换保留。
   - 只切换静态 UI 文案。
   - 不翻译博客、项目、经历、成就的具体内容。

7. GitHub README 自动解析仍作为当前开发目标。
   - 但不通过后端实现。
   - 改为本地脚本或开发工具能力：输入 GitHub 仓库链接，生成或更新本地 Project Markdown 文件。

8. Contact 不再作为独立联系表单模块。
   - Contact 内容合并到 About 或页面底部静态信息区。
   - 不保留真实表单提交。

9. Netlify 部署方式采用 GitHub 自动部署。
   - 推送到 GitHub 后，Netlify 自动执行构建并发布。
   - 本地仍可运行构建命令验证。

10. 文档保留“未来可扩展后端”章节。
    - 简要说明未来可升级为 FastAPI + PostgreSQL。
    - 不作为当前版本开发目标。

## 3. 产品目标

### 3.1 当前版本目标

1. 保持现有作品集首页的视觉风格和响应式体验。
2. 增加博客 / 成长记录能力。
3. 使用 Markdown 管理 Experience 和 Project。
4. 使用 TypeScript 数据维护 Achievement。
5. 保留静态 UI 中英文切换。
6. 移除 Contact 独立表单逻辑，改为静态信息展示。
7. 支持本地编辑内容后自动部署到 Netlify。
8. 提供本地 GitHub README 解析工具，辅助生成 Project Markdown。

### 3.2 非目标

当前版本不做：

1. 线上后端服务。
2. 线上数据库。
3. 访客注册和登录。
4. 管理员登录。
5. 可视化后台。
6. 在线增删改查。
7. 在线 Contact 表单提交。
8. 评论、点赞、访问统计。
9. 在线 CMS。
10. AI 自动翻译全站内容。

## 4. 核心使用场景

### 4.1 对外展示

访客打开网站后，可以看到：

1. 个人介绍。
2. 技术方向。
3. 学习和项目经历。
4. 项目作品。
5. 成就记录。
6. 博客或成长记录文章。
7. 基础静态信息，如 GitHub、Email、状态说明。

访客只浏览内容，不写入任何数据。

### 4.2 本地成长记录

站点拥有者在本地维护内容：

1. 新增一篇成长记录 Markdown。
2. 新增或更新一个项目 Markdown。
3. 修改成就数据文件。
4. 本地运行测试和构建。
5. 推送到 GitHub。
6. Netlify 自动部署。

该流程适合持续记录学习过程、项目迭代和阶段成果。

## 5. 页面结构

### 5.1 首页

首页继续保留长滚动结构：

```txt
Home / Hero
About
Experience
Projects
Achievements
Blog / Notes
Footer Info
```

调整说明：

1. Contact 独立 section 可以取消。
2. 原 Contact 的 Email、GitHub、Resume、Status、Location 合并到 About 或 Footer Info。
3. 新增 Blog / Notes 区域，用于展示最近的成长记录。

### 5.2 项目详情页

路由：

```txt
/project/:id
```

项目详情页内容来自 Project Markdown，包含：

1. Project Hero。
2. Overview。
3. My Role。
4. Core Features。
5. Tech Stack。
6. Challenges & Solutions。
7. Result。
8. Back Home。

### 5.3 博客 / 成长记录页

建议新增：

```txt
/blog
/blog/:slug
```

列表页展示成长记录文章摘要。详情页展示 Markdown 正文。

如果当前阶段希望更轻量，可以先只在首页展示最近几条记录，后续再增加完整博客列表页。

## 6. 内容维护方案

### 6.1 Experience

Experience 从单纯时间线升级为“成长记录时间线”。

推荐目录：

```txt
src/content/experiences/
├─ 2026-05-ai-ppt-learning.md
├─ 2026-04-frontend-refactor.md
└─ 2026-03-competition-review.md
```

每个文件包含 frontmatter：

```md
---
title: "AI PPT 生成系统阶段记录"
period: "2026-05"
role: "AI Application Developer"
category: "Project Learning"
summary: "记录 AI PPT 生成系统从需求拆解到 MVP 实现的阶段过程。"
tags:
  - AI Application
  - Vue3
  - MVP
sortOrder: 10
isPublished: true
---

这里写具体成长记录、复盘、遇到的问题和下一步计划。
```

### 6.2 Project

Project 使用 Markdown 文件维护。

推荐目录：

```txt
src/content/projects/
├─ ai-ppt-generator.md
├─ cultural-ai-creation-platform.md
└─ study-monitor.md
```

每个文件包含 frontmatter：

```md
---
id: "ai-ppt-generator"
title: "AI PPT Generator"
type: "AI Web Application"
summary: "一个面向论文和文档的 AI 演示文稿生成系统。"
stack:
  - Vue3
  - FastAPI
  - LLM API
featured: true
coverDoodle: "/images/doodles/browser-plant.png"
githubUrl: ""
demoUrl: ""
sortOrder: 10
isPublished: true
---

## Overview

项目概览内容。

## My Role

我的职责。

## Core Features

核心功能。

## Challenges & Solutions

难点与解决方案。

## Result

项目结果和后续改进。
```

### 6.3 Achievement

Achievement 保持 TypeScript 数据文件：

```txt
src/data/achievements.ts
```

维护原则：

1. 只记录可以对外展示的阶段成果。
2. 文案不夸大。
3. 图标继续使用 PNG 装饰素材。
4. 使用 `sortOrder` 控制展示顺序。
5. 使用 `isPublished` 控制是否展示。

### 6.4 静态 UI 文案

全局中英文静态文案继续维护在：

```txt
src/i18n/messages.ts
```

范围：

1. 导航。
2. section 标题。
3. 按钮。
4. 空状态。
5. 固定提示。
6. Footer 静态信息标签。

不翻译：

1. Markdown 正文。
2. 项目详情内容。
3. 成长记录内容。
4. 成就描述。

## 7. GitHub README 自动解析

当前版本保留 GitHub README 自动解析目标，但实现方式从后端 API 改为本地脚本。

目标：

```txt
输入 GitHub 仓库链接
  ↓
本地脚本拉取 README
  ↓
解析项目标题、简介、技术栈、功能点
  ↓
生成 src/content/projects/*.md
  ↓
本地检查后提交部署
```

推荐脚本位置：

```txt
scripts/import-github-project.ts
```

命令示例：

```bash
npm run import:github -- https://github.com/owner/repo
```

实现原则：

1. 只作为辅助生成草稿。
2. 生成后必须人工检查。
3. 不自动发布。
4. 不需要后端。
5. 不需要数据库。
6. 如需 GitHub Token，只放在本地 `.env`，不提交仓库。

## 8. 技术架构

### 8.1 当前主线架构

```txt
Vue3 + Vite + TypeScript
  ↓
Markdown / TS 本地内容
  ↓
构建为静态文件 dist
  ↓
Netlify 静态部署
```

技术栈：

```txt
Vue3
Vite
TypeScript
Vue Router
Markdown 内容
CSS Variables
原生 CSS
Netlify
```

### 8.2 不再作为当前主线的架构

以下内容不进入当前版本：

```txt
FastAPI 后端
PostgreSQL 数据库
线上 CRUD API
API_WRITE_TOKEN
Render 后端部署
Neon / Supabase 数据库部署
```

这些能力可以保留在未来扩展章节，不影响当前静态站目标。

## 9. 部署方案

部署方式采用 GitHub 自动部署到 Netlify。

流程：

```txt
本地修改内容
  ↓
npm run test
  ↓
npm run build
  ↓
git commit
  ↓
git push
  ↓
Netlify 自动构建
  ↓
发布线上网站
```

Netlify 配置：

```txt
Build command: npm run build
Publish directory: dist
```

SPA 路由 fallback：

```txt
/* /index.html 200
```

当前版本不需要在 Netlify 配置数据库连接或后端 API 密钥。

## 10. 开发阶段计划

### 阶段一：产品目标回退与文档同步

目标：

1. 将项目目标明确为静态博客 + 作品集。
2. 将后端和数据库移入未来扩展。
3. 更新 README 和 AGENT.md。

验收标准：

1. 文档不再把后端部署作为当前目标。
2. 当前开发主线清晰指向静态站。

### 阶段二：Markdown 内容系统

目标：

1. 新增 `src/content/experiences`。
2. 新增 `src/content/projects`。
3. 支持读取 frontmatter。
4. 将 Experience 和 Project 从 TS 数据迁移到 Markdown。

验收标准：

1. 新增 Markdown 文件即可新增成长记录。
2. 新增 Markdown 文件即可新增项目。
3. 项目详情页能渲染 Markdown 正文。

### 阶段三：博客 / 成长记录页面

目标：

1. 新增 Blog / Notes 展示入口。
2. 支持成长记录列表。
3. 支持成长记录详情页。

验收标准：

1. 首页展示最近成长记录。
2. `/blog` 可查看全部记录。
3. `/blog/:slug` 可查看单篇记录。

### 阶段四：GitHub README 本地导入脚本

目标：

1. 输入 GitHub 仓库链接。
2. 拉取 README。
3. 生成 Project Markdown 草稿。

验收标准：

1. 合法 GitHub 仓库可生成项目草稿。
2. README 不存在时给出清晰错误。
3. 生成结果不会自动覆盖已有项目，除非显式确认。

### 阶段五：Netlify 自动部署稳定化

目标：

1. 使用 GitHub 绑定 Netlify。
2. 推送后自动部署。
3. 确认 SPA 路由刷新正常。
4. 完善 README 中的内容维护和部署流程。

验收标准：

1. GitHub push 后 Netlify 自动部署。
2. 首页和详情页线上可访问。
3. 本地内容维护流程清晰。

## 11. 风险与取舍

### 11.1 放弃线上后端的取舍

收益：

1. 部署简单。
2. 免费成本更可控。
3. 国内访问主要只受 Netlify 影响。
4. 没有数据库维护成本。
5. 没有后端安全风险。

代价：

1. 不能在线编辑内容。
2. 每次修改内容都要重新部署。
3. GitHub README 导入只能本地执行。

该取舍符合当前“个人展示 + 本地成长记录”的实际需求。

### 11.2 Markdown 内容系统的风险

风险：

1. frontmatter 写错会导致构建失败。
2. Markdown 文件过多后需要更好的分类和索引。

控制方式：

1. 为内容结构写 TypeScript 类型和校验。
2. 在测试中检查必填字段。
3. 在 README 中提供内容模板。

## 12. 成功标准

当前版本完成后应达到：

1. 网站是静态博客 + 作品集。
2. 首页保持现有视觉风格。
3. Experience 由 Markdown 本地维护。
4. Project 由 Markdown 本地维护。
5. Achievement 由 TypeScript 数据维护。
6. 支持博客 / 成长记录展示。
7. 支持静态 UI 中英文切换。
8. Contact 已合并为静态信息，不保留独立表单。
9. GitHub README 可通过本地脚本生成项目草稿。
10. GitHub push 后 Netlify 自动部署。
11. 不依赖线上后端和数据库。

## 13. 未来扩展方向

未来如果本地文件维护无法满足需求，可以升级为：

```txt
Vue 前端
  ↓
FastAPI 后端
  ↓
PostgreSQL 数据库
```

可扩展能力：

1. `/admin` 可视化管理后台。
2. 管理员登录。
3. Experience / Project / Achievement 在线 CRUD。
4. GitHub README 在线解析。
5. AI 自动总结项目。
6. AI 自动生成英文介绍。
7. 图片上传和封面管理。
8. 访问统计。

这些能力不属于当前版本目标。

