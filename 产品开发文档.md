# 个人作品集网站产品开发文档

更新时间：2026-05-04

## 1. 产品定位

本项目从静态个人作品集网站升级为“个人自用内容管理型作品集网站”。前台继续保持当前设计风格，用于展示 Shroud 的个人简介、学习经历、项目作品、成就记录和静态联系信息；后端用于个人维护内容，支持 Experience、Project、Achievement 的数据管理，并为未来 GitHub README 自动解析项目内容提供能力。

本项目不面向多用户运营，不开发访客注册、用户登录、评论、留言、后台账号体系或真实联系表单。所有编辑能力只服务站点拥有者本人。

## 2. 当前选择结论

### 2.1 已确认方案

1. 全局中英文切换只作用于静态页面文案。
   - 包括导航、section 标题、按钮文案、固定说明文字、空状态、提示信息等。
   - Project、Experience、Achievement 的具体内容暂时不做中英文切换。

2. 后端部署方式采用“前后端分离”。
   - 前端继续部署在 Netlify。
   - 后端使用 Python + FastAPI，独立部署到 Render、Railway、Fly.io 或其他支持 Python 服务的平台。
   - 数据库使用 Supabase Postgres 或 Neon Postgres。

3. 登录和多用户相关功能暂不开发。
   - 不做访客登录。
   - 不做完整账号系统。
   - 不做面向其他用户的权限体系。

4. 中英文内容维护方式采用轻量前端方案。
   - 静态页面文案先在前端维护。
   - 动态内容仍以中文为主。

5. 暂时不开发可视化管理后台页面。
   - 不新增 `/admin` 页面。
   - 内容管理先通过 FastAPI 自动生成的 Swagger UI、API 工具或后续脚本完成。

6. Project GitHub README 自动解析进入本轮规划。
   - 目标是输入 GitHub 仓库链接后，后端拉取 README，并自动解析项目标题、简介、技术栈、功能点等字段。
   - 解析结果需要保存为 Project 数据。

## 3. 产品目标

### 3.1 短期目标

1. 保留现有前台视觉和浏览体验。
2. 将静态数据逐步迁移为 API 数据源。
3. 支持 Experience、Project、Achievement 的增删查改。
4. Contact 模块取消表单，改为静态信息展示。
5. 增加全局中英文切换，仅切换静态 UI 文案。
6. 提供 GitHub README 自动解析并创建 Project 的后端接口。
7. 前端可在 Netlify 部署，后端可独立部署。

### 3.2 非目标

本阶段不做以下功能：

1. 用户注册。
2. 访客登录。
3. 多角色权限系统。
4. 在线可视化管理后台。
5. 真实联系表单提交。
6. 评论、点赞、访问统计。
7. AI 自动翻译全站内容。
8. CMS 富文本编辑器。
9. 文件上传管理系统。

## 4. 功能范围

### 4.1 前台展示功能

前台继续包含以下页面和模块：

1. 首页 `/`
   - Home / Hero
   - About
   - Experience
   - Projects
   - Achievements
   - Contact

2. 项目详情页 `/project/:id`
   - Project Hero
   - Overview
   - My Role
   - Core Features
   - Tech Stack
   - Challenges & Solutions
   - Result
   - Back Home

3. 全局中英文切换
   - 仅切换静态 UI 文案。
   - 动态内容不翻译。
   - 语言状态可保存在 localStorage。

4. Contact 静态信息模块
   - 展示 Email。
   - 展示 GitHub。
   - Resume 保持占位或禁用。
   - 不展示可提交表单。
   - 不发送网络请求。

### 4.2 后端内容管理功能

后端需要提供以下内容管理能力：

1. Experience 管理
   - 查询经历列表。
   - 新增经历。
   - 编辑经历。
   - 删除经历。
   - 支持排序。
   - 支持是否公开展示。

2. Project 管理
   - 查询项目列表。
   - 查询项目详情。
   - 新增项目。
   - 编辑项目。
   - 删除项目。
   - 设置 featured。
   - 支持排序。
   - 支持 GitHub 链接和 Demo 链接。
   - 支持项目详情字段。

3. Achievement 管理
   - 查询成就列表。
   - 新增成就。
   - 编辑成就。
   - 删除成就。
   - 支持排序。
   - 支持是否公开展示。

4. GitHub README 自动解析
   - 输入 GitHub 仓库 URL。
   - 后端解析 owner 和 repo。
   - 调用 GitHub API 获取 README。
   - 将 README Markdown 转为纯文本或结构化内容。
   - 尝试解析项目名称、简介、技术栈、核心功能、挑战与结果。
   - 生成 Project 草稿数据并保存。

## 5. 后端必要性判断

### 5.1 不需要后端的部分

1. 静态 UI 文案中英文切换。
2. Contact 静态信息展示。
3. 页面基础响应式。
4. 前台视觉样式和装饰图。

### 5.2 需要后端的部分

1. Experience 可添加、编辑、删除。
2. Project 可添加、编辑、删除。
3. Achievement 可添加、编辑、删除。
4. Project 数据从 GitHub README 自动生成。
5. 将内容从 `src/data/*.ts` 迁移到数据库并长期维护。

## 6. 技术架构

### 6.1 前端

技术栈：

```txt
Vue3
Vite
TypeScript
Vue Router
CSS Variables
原生 CSS
```

职责：

1. 展示作品集页面。
2. 读取公开 API 数据。
3. 处理静态 UI 文案语言切换。
4. 保持现有纸感、手绘、极简视觉风格。

### 6.2 后端

技术栈：

```txt
Python 3.10+
FastAPI
Pydantic
SQLAlchemy 或 SQLModel
Alembic
PostgreSQL
```

职责：

1. 提供公开读取接口。
2. 提供个人自用 CRUD 接口。
3. 提供 GitHub README 解析接口。
4. 负责数据校验。
5. 负责数据库访问。
6. 负责日志记录。

### 6.3 数据库

推荐：

```txt
Supabase Postgres 或 Neon Postgres
```

原因：

1. 适合个人项目。
2. 免费额度足够 MVP 使用。
3. 与独立 FastAPI 部署兼容。
4. 后续迁移成本低。

### 6.4 部署

部署结构：

```txt
Netlify
  └─ Vue3 前端静态站

Render / Railway / Fly.io
  └─ FastAPI 后端服务

Supabase / Neon
  └─ PostgreSQL 数据库
```

前端通过环境变量配置 API 地址：

```txt
VITE_API_BASE_URL=https://your-api-domain.example.com
```

后端通过环境变量配置：

```txt
DATABASE_URL=postgresql+psycopg://...
GITHUB_TOKEN=可选
API_WRITE_TOKEN=个人写入令牌
```

## 7. 安全策略

由于本阶段不开发登录系统，但又需要个人自用写入能力，因此不能让写入接口裸奔。

推荐使用简单写入令牌方案：

1. 所有公开读取接口不需要鉴权。
2. 所有新增、编辑、删除、GitHub 解析保存接口必须携带写入令牌。
3. 写入令牌从后端环境变量读取。
4. 前端公开页面不保存写入令牌。
5. 本阶段通过 Swagger UI、Postman、Apifox 或 curl 调用写入接口。

请求头示例：

```txt
Authorization: Bearer <API_WRITE_TOKEN>
```

该方案不是完整账号系统，但足够满足“只供自己使用、暂不开发登录”的阶段目标。

## 8. 数据模型草案

### 8.1 Experience

字段建议：

```txt
id
period
role
org
description
sortOrder
isPublished
createdAt
updatedAt
```

### 8.2 Project

字段建议：

```txt
id
slug
title
type
summary
overview
roleItems
features
techStack
challenges
result
coverDoodle
githubUrl
demoUrl
featured
sortOrder
isPublished
createdAt
updatedAt
```

### 8.3 Achievement

字段建议：

```txt
id
title
description
icon
sortOrder
isPublished
createdAt
updatedAt
```

### 8.4 GitHubImportDraft

该模型可以不落库，也可以作为调试记录保存。

字段建议：

```txt
repoUrl
owner
repo
readmeMarkdown
parsedTitle
parsedSummary
parsedTechStack
parsedFeatures
parsedResult
createdAt
```

## 9. API 设计草案

### 9.1 公开读取接口

```txt
GET /api/experiences
GET /api/projects
GET /api/projects/{slug}
GET /api/achievements
```

公开接口只返回 `isPublished = true` 的数据，并按 `sortOrder` 排序。

### 9.2 个人写入接口

以下接口需要写入令牌：

```txt
POST /api/experiences
PATCH /api/experiences/{id}
DELETE /api/experiences/{id}

POST /api/projects
PATCH /api/projects/{id}
DELETE /api/projects/{id}

POST /api/achievements
PATCH /api/achievements/{id}
DELETE /api/achievements/{id}
```

### 9.3 GitHub README 解析接口

以下接口需要写入令牌：

```txt
POST /api/projects/import/github
```

请求体：

```json
{
  "repoUrl": "https://github.com/owner/repo"
}
```

响应体：

```json
{
  "title": "项目名称",
  "summary": "项目简介",
  "techStack": ["Vue3", "FastAPI"],
  "features": ["核心功能 1", "核心功能 2"],
  "githubUrl": "https://github.com/owner/repo"
}
```

## 10. 前端改造方案

### 10.1 数据来源改造

当前数据来自：

```txt
src/data/profile.ts
src/data/nav.ts
src/data/projects.ts
src/data/experiences.ts
src/data/achievements.ts
```

目标状态：

1. `profile.ts` 和 `nav.ts` 继续保留在前端。
2. 静态 UI 文案使用前端 i18n 配置维护。
3. `projects.ts`、`experiences.ts`、`achievements.ts` 逐步改为 API 数据源。
4. 可以保留本地 fallback 数据，防止后端不可用时页面空白。

### 10.2 中英文切换

实现范围：

1. 导航文案。
2. section 标题。
3. 按钮文案。
4. 静态提示。
5. Contact 固定标签。
6. Not Found 文案。

不实现范围：

1. Project 具体标题和描述翻译。
2. Experience 具体描述翻译。
3. Achievement 具体描述翻译。
4. GitHub README 自动翻译。

建议文件：

```txt
src/i18n/messages.ts
src/composables/use-locale.ts
```

### 10.3 Contact 模块

调整为静态信息展示：

```txt
Email
GitHub
Resume
Status
Location
```

表单字段和 Send Message 按钮应删除。

## 11. 后端目录结构建议

建议在项目根目录新增：

```txt
backend/
├─ app/
│  ├─ api/
│  │  ├─ routes/
│  │  │  ├─ achievements.py
│  │  │  ├─ experiences.py
│  │  │  ├─ github_import.py
│  │  │  └─ projects.py
│  │  └─ deps.py
│  ├─ core/
│  │  ├─ config.py
│  │  └─ security.py
│  ├─ model/
│  │  ├─ achievement.py
│  │  ├─ experience.py
│  │  └─ project.py
│  ├─ repository/
│  │  ├─ achievement_repository.py
│  │  ├─ experience_repository.py
│  │  └─ project_repository.py
│  ├─ schema/
│  │  ├─ achievement.py
│  │  ├─ experience.py
│  │  ├─ github_import.py
│  │  └─ project.py
│  ├─ service/
│  │  ├─ achievement_service.py
│  │  ├─ experience_service.py
│  │  ├─ github_import_service.py
│  │  └─ project_service.py
│  ├─ db.py
│  └─ main.py
├─ tests/
├─ pyproject.toml
└─ README.md
```

该结构符合项目规则中的后端分层要求：

1. `api` 负责请求解析和响应封装。
2. `service` 负责业务逻辑。
3. `repository` 负责数据库访问。
4. `schema` 负责请求和响应数据校验。
5. `model` 负责 ORM 模型定义。

## 12. 开发阶段计划

### 阶段一：前端轻量改造

目标：

1. Contact 改为静态信息展示。
2. 增加全局语言状态。
3. 静态 UI 文案支持中文和英文。

验收标准：

1. 页面可以切换中文 / 英文 UI。
2. 动态内容不被翻译。
3. Contact 不再展示表单。
4. 不影响现有视觉风格。

### 阶段二：后端基础项目搭建

目标：

1. 新增 FastAPI 后端目录。
2. 建立数据库连接。
3. 建立 Experience、Project、Achievement 模型。
4. 提供公开读取接口。

验收标准：

1. FastAPI 服务可本地启动。
2. Swagger UI 可访问。
3. 公开读取接口返回数据。
4. 数据库迁移可执行。

### 阶段三：个人写入接口

目标：

1. 增加 Experience CRUD。
2. 增加 Project CRUD。
3. 增加 Achievement CRUD。
4. 增加写入令牌校验。

验收标准：

1. 未携带写入令牌时无法修改数据。
2. 携带正确写入令牌时可以新增、编辑、删除数据。
3. 所有输入经过 Pydantic 校验。
4. API 层不直接操作数据库。

### 阶段四：前端接入 API

目标：

1. 首页 Experience 从 API 获取。
2. 首页 Projects 从 API 获取。
3. 首页 Achievements 从 API 获取。
4. 项目详情页从 API 获取项目详情。

验收标准：

1. API 可用时显示数据库内容。
2. API 不可用时有合理空状态或 fallback。
3. featured 项目仍只展示前三个。
4. 项目详情页路径正常。

### 阶段五：GitHub README 自动解析

目标：

1. 输入 GitHub 仓库 URL。
2. 后端拉取 README。
3. 后端解析项目字段。
4. 生成并保存 Project 数据。

验收标准：

1. 合法 GitHub 仓库链接可以解析。
2. README 不存在时返回清晰错误。
3. 解析出的 Project 可被前台展示。
4. 不把 GitHub Token 暴露到前端。

### 阶段六：部署准备

目标：

1. Netlify 前端配置 API 地址。
2. 后端平台配置数据库连接和写入令牌。
3. 数据库迁移和种子数据可执行。
4. README 更新部署说明。

验收标准：

1. 前端可部署到 Netlify。
2. 后端可独立部署。
3. 前端能访问线上后端 API。
4. 写入接口不公开裸奔。

## 13. 风险与取舍

### 13.1 不做登录但提供写入接口的风险

风险：

1. 写入令牌泄露后，别人可以修改内容。
2. Swagger UI 暴露接口结构。

控制方式：

1. 写入令牌只放在后端环境变量和本地工具中。
2. 前端不保存写入令牌。
3. 写入令牌足够长。
4. 后续如有需要，再升级为管理员登录。

### 13.2 GitHub README 自动解析的不确定性

风险：

1. 不同仓库 README 结构差异很大。
2. 自动解析结果可能不稳定。
3. 技术栈和功能点可能识别不完整。

控制方式：

1. 先做规则解析，不接 AI。
2. 保留手动编辑 Project 的能力。
3. 自动解析只作为创建草稿的辅助能力。

### 13.3 前后端分离带来的部署复杂度

风险：

1. 需要分别部署前端、后端、数据库。
2. 需要处理 CORS。
3. 需要管理环境变量。

控制方式：

1. 前端继续保持 Netlify 静态部署。
2. 后端只承担内容接口，不承担页面渲染。
3. 后端允许的 CORS 域名只配置前端正式域名和本地开发地址。

## 14. 成功标准

项目完成后应达到：

1. 前台作品集页面保持现有设计质量。
2. 静态 UI 支持中文和英文切换。
3. Experience 可以通过 API 新增、编辑、删除和展示。
4. Project 可以通过 API 新增、编辑、删除和展示。
5. Achievement 可以通过 API 新增、编辑、删除和展示。
6. Contact 已改为静态信息展示模块。
7. GitHub README 可以自动解析并生成 Project 数据。
8. 前端可部署到 Netlify。
9. 后端可独立部署并连接 PostgreSQL。
10. 写入接口需要令牌，不允许任何人随意修改内容。

## 15. 后续可扩展方向

以下功能暂不开发，但可以作为后续版本：

1. `/admin` 可视化管理后台。
2. 管理员登录。
3. AI 自动总结 GitHub README。
4. AI 自动生成英文项目介绍。
5. 图片上传和封面管理。
6. 访问统计。
7. 博客模块。
8. 简历 PDF 在线管理。

